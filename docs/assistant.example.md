# assistant.md — personal preferences for working with an AI assistant

Copy to `assistant.md` at the repository root (git-ignored; `CLAUDE.md` reads it). It sets
conversation language, tone and workflow; it cannot relax `AGENTS.md`.

## Preferences

- Talk to me in English.
